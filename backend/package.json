{
  "name": "jade-spaceapps-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest"
  },
  "dependencies": {
    "fastapi": "^0.95.0",
    "uvicorn": "^0.22.0",
    "pydantic": "^1.10.9"
  },
  "devDependencies": {
    "pytest": "^7.4.0",
    "httpx": "^0.24.1"
  }
}
